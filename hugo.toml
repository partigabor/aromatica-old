baseURL = 'https://partigabor.github.io/aromatica/'
languageCode = 'en-us'
title = 'aromatica'

# Change the default theme to be use when building the site with Hugo
theme = "hugo-theme-relearn"

# publishDir = "docs"

# For search functionality
[outputs]
home = [ "HTML", "RSS", "SEARCH"]